# Core inference
numpy==1.24.4
opencv-python>=4.6,<5
Pillow
PyYAML
easydict
scipy
ultralytics==8.3.141
zstandard

# ReID
tabulate==0.9.0
termcolor==3.1.0
tqdm
yacs==0.1.8

# ONNX / TensorRT integration
# PyTorch, torchvision, and TensorRT must match JetPack.
# install_root.sh validates them and installs torch2trt separately.
onnx
onnxoptimizer
onnxsim
pycuda

# Utilities
gdown
imageio
matplotlib

# Protocols
eclipse-zenoh==1.9.0
